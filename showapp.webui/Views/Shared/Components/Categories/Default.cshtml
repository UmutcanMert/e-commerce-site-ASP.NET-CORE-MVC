@using shopapp.entity
@model List<Category>


<div class="list-group">

    <a href="/" class="list-group-item list-group-item-action">Tüm Kategoriler</a>

    @foreach(var item in Model)
    {
        <a asp-controller="Shop" asp-action="List" asp-route-category="@item.Url" class="btn btn-light list-group-item list-group-item-action @(ViewBag.SelectedCategory == item.Url?"active":"")">@item.Name</a>
    }
    
    @await Html.PartialAsync("_search")
</div>